name: iarh_frontend
description: IARH - interface web de simulation d'entretien IA
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.11.0
  flutter_secure_storage: ^10.3.1
  go_router: ^17.3.0
  video_player: ^2.10.0
  just_audio: ^0.10.5
  livekit_client: ^2.9.0
  url_launcher: ^6.3.2
  http: ^1.4.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
